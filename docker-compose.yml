version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: biotrace-mongo
    ports:
      - "27017:27017"
    volumes:
      - biotrace_data:/data/db
    restart: unless-stopped

volumes:
  biotrace_data:
